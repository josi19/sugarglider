version: 2
updates:
  # The app itself has zero dependencies, so GitHub Actions is the only
  # ecosystem here — and the one that quietly goes stale otherwise.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    commit-message:
      prefix: "ci"
    groups:
      actions:
        patterns: ["*"]
